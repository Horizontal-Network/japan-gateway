import 'package:flutter/material.dart';

class DestinationsMobile extends StatelessWidget {
  const DestinationsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Genie Mobile"));
  }
}
