import 'package:flutter/material.dart';

class ExperiencesMobile extends StatelessWidget {
  const ExperiencesMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Genie Mobile"));
  }
}
