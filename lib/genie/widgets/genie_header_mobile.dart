import 'package:flutter/material.dart';

class GenieHeaderMobile extends StatelessWidget {
  const GenieHeaderMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
