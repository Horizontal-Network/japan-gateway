import 'package:flutter/material.dart';

class GenieMobile extends StatelessWidget {
  const GenieMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Genie Mobile"));
  }
}
