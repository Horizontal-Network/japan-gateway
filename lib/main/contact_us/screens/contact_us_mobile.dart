import 'package:flutter/material.dart';

class ContactUsMobile extends StatelessWidget {
  const ContactUsMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Contact Us Page - Mobile"));
  }
}
