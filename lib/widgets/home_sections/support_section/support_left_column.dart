import 'package:flutter/material.dart';

class SupportLeftColumn extends StatelessWidget {
  const SupportLeftColumn({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Image.asset(
          'images/tmp/support-photo-placeholder.png'
        ), 
      ],
    );
  }
}
